workspace(name = "sunny_circuit")

# 使用本地 Zig 工具链
load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")

# 规则集用于 Zig (使用 rules_zig 或通用规则)
# 注意：这是一个最小化配置，主要使用 zig 命令行工具

# 设置 Bazel 使用本地 Zig
# 实际 Zig 编译通过 repository_rule 或 genrule 调用 zig build
